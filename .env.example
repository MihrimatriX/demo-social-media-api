# Kopyalayın: copy .env.example .env  (docker compose --env-file .env kullanıyorsanız)
# Yerel dotnet run için User Secrets veya appsettings.Development.json tercih edin.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=demosocialmedia

JWT_KEY=change-me-min-32-chars-for-hmac-sha256!!

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123
